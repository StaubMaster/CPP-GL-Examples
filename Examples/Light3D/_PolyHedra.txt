
currently have a PolyHedra Manager for regular PolyHedra
and Indicator PolyHedra

their Main data is the same
only the Instance stuff is different

how to combine ?

Manager
{
	Container of PolyHedraManager (per PolyHedra)
	{
		PolyHedra
		Buffer
		Textures
	}

	Container of ObjectManager (per ObjectData)
	{
		Shader
		Contianer of PolyHedraObjectManager (per PolyHedra)
		{
			Pointer of PolyHedraManager
			Buffer
			Container of ObjectData
			Container of InstanceData
		}
	}
}

handle Loading / Generating in the Manager
so it checks if the PolyHedra is already loaded / generate

spheres take parameters, how would these be checked ?
	put parameters in the Name
	generate that name, then look for it



if a new PolyHedra is put into the Manager
dont put it into all ObjectManager things ?

wait until a ObjectManager wants a PolyHedra,
then make PolyHedraObjectManager

delete unused ?
if a PolyHedraObjectManager has no Objects (for some time),
delete it



Draw order
some Indicators, like Move stuff should be drawn in front of everything
2D Projected PolyHedra too

dont allow dynamic making of ObjectManager
specify all at the beginning
have named Pointers to them
so the user needs to specify how/when to draw them
