
Angles:
	can be represented in different ways

Rotation Matrix:
	currently used for shaders
	could be combined with Position and Scaling as well
	could also include other distortions ?

Euler Angles:
	a sequance if Rotations
	currently Angle3D
	internally stores a Rotation Matrix
	the Matrix is used for actual calculations
	store Matrix seperatly ?
	turn Matrix back in to Euler Angles ?

Euler Axis:
	currently not explicitly used
	would just be a Point
	implicitly used in 2D
	a Axis around which a object rotates
	the length of the axis is how much it rotates
	could be directly turned into Rotation Matrix

Euler Angles <-> Euler Axis ?

Changes:
	PointN:
		rename to VectorN ?
	AngleN:
		rename to EulerAngleN
		store the order of Rotations ?
		I only need EulerAngle3D ?
		just use a float for 2D ?
		make EulerAngle2D a wrapper for a float, for ease of use
	TransN:
		internally store VectorN
		internally store EulerAngleN
		internally store VectorN for stretching ?
		can be turned directly into a Transformation Matrix
	also:
		all objects that store TransN should also store Transformation Matrix
		TransN is only really there to see the Components
		or should TransN store the Matrix ?
		no:
			right now AngleN stores the Matrix,
			but if TransN also Stores the Matrix
			then that would be redundand
			so if something else changes the Transformation Matrix
			then the one int TransN would be redundand
		but:
			what if I just want to rotate, then the one in AngleN would be useful
			just manually rotate, this would be slow, but it shouldnt happen that often ?
			except Collision uses pure Rotation ?



Container::Block
	takes a Block size (1024)
	then allocates in Blocks
	so that it has to reallocate only a few times

	this is similar to Binary
	but Binary reallocates less, the more it has



Point
	Round()			round all values
	RoundPositive()	round positive (ceiling)
	RoundNegative()	round negative (floor)

	Round(float)	round based on a value (round(val / param)) * param
	RoundPls(float)	
	RoundMns(float)	

	normalize() with length 0 should not NaN

Angle
	also Round() functions
	take Angle isntead of float due to Rad vs Deg

Angle and Trans operators
	currently to rotate an Angle with a Point
	it would make more sense to multiply a Point with a Angle
	same for Trans

AngleN
	give Degree variant
	make refular n float constructor private
	make Radian function



Graphics stuff
	.Create()
	.Delete()
		take bool for if it should Log
		otherwise, only Log Errors, like Shader not Compiling

Attribute Buffer
	currently, there is a function to set Attributes
	set them when .Change() is called
	do Attributes know their Buffer ?
	they need to Bind it to change the Attributes
	they currently dont know
	but Uniforms know, and use a Binary Container
	do the same in Attribute Buffer



View2D
	turn View into Matrix
	use Matrix for Uniform



FunctionPointer
	rename .Change() to .Assign()
	maybe operator=
	the functions requires a template, 
	so operator= wont work



UserParameter
	UserParameter::KeyBoard::Keys:: is long
	change to just Keys
	same with MouseButtons
	also, KeyManager should be able to Index Keys / MouseButtons directly

	make enums for Keys and MouseButtons so they can be converted to strings

my	UserParameter::Mouse::Scroll params
C#	MouseScrollEvent
C#	MouseScrollEventArgs e
	Mouse::Scroll params

	params vs args vs e ?

Mouse::Scroll params.Code is 0

rename UserParameter to just User ?



Drag
	Parameters
	should give Status
		Start
		During
		End



Transform
Angle Trans View
	right now, Transformation is done with operator * and /
	but those are vagua
	what would be better names ? (preferably short)

	refular
	reverse

	normal
	forward
	backward

	multiply
	divide

	Transform is the correct word
	Transform
	Transpose

a Matrix cannot be "reversed"
and cannot be used to "un-transform"

if I want to View-transform backwards
I have to use the components in reverse, and create a new Matrix

for View, just have the function describe what it does ?
	RelativeToAbsolute
	AbsoluteToRelative

	ToAbsolute
	ToRelative


