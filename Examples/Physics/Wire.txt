
each Polygon Should have a WireFrame

also have a Oriented Box for each Polygon





Graphics:

a Polygon is just a Polygon
a Physics Object is a Polygon at with a Orientation
there can be different Physics Objects with the same or a different Polygon

right now I call this the MainInstance (maybe just Main ?)
and the individual Objects are Instances



each MainInstance should have
{
	PolyGon
	TransBox

	PolyGon_Buffer
	PolyGon_WireBuffer
	TransBox_WireBuffer

	Binary<>	InstanceData
}





How to Show/Hide WireFrame (or just any Elements)
have seperate Instances for each ?

have Instances be the "Main" Container
so of the actual Objects

but then every Frame, make seperate Lists for each "Type"
so have Toggles for PolyGon, WireFrame and WireFrameBox
then make a Container as big as Instances (or just Binary)
and then fill that with the actual Buffer Instances
