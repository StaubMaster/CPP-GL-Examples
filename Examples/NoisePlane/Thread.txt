
Draw Thread should just Draw
seperate Threads for
	Input
	UI

Input
	callback only happen in the main thread, the same that draws ?
	.Tick for Input Managers could be done from other Thread
	but make sure there is no conflict
	thats kind of everything that does ?

	getting Input suck as checking Keys/Mouse Position should be doable from other threads
	need to lock when getting Key ?
	also have a way to lock Input, get Keys and then unlock
	just use a recursive Mutex

UI
	all the internal stuff can be done from anywhere
	Callbacks are called by Window Callbacks which are called by glfw Callbacks
	so those need to be queued
	have 2 tickes
	one that calls Callbacks as fast as possible
	and one the updates the Controls

	richt now the UI stuff is the bare minimum
	add more stuff ?
	make it work better ?


