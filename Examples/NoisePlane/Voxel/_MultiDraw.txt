
put everything into 1 Buffer

use MultiDraw to draw Buffers
there is no Instancing with MultiDraw

making non Instance would mean 3 * 4 = 12 extra Bytes per Vertex
it is currently 8 Bytes so it would be more then twice





putting into 1 Buffer is basically EntryContainer
MultiDraw basically takes Entrys but rearanged ?

EntryContainer has its owm Memory
this would preallocate the Buffer and map to that

so the other thread makes BufferData Block
which then gets put into one thing in the Buffer

what if Buffer is full ?
if the Empty Space summed up would be enough
	compress and insert
else
	make another Buffer
	


