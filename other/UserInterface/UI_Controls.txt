what to improve

Removing Children
there are some other things that could be done delayed



use Images instead of generic Boxes ?
this would allow rounden corners
this is unneeded



resizing/moving
only with Forms
moving should be easy
resizing should show the resizing cursors
maybe have customn cursors
glfw does have a Vertical and Horizontal resize cursor
but not a corner resize, so that would be needed anyway

how to know resize ?
just check if the cursor is hovering the boarder ?
real windows have a bit of space outside of the boarder where resizing is done
implement with boarder for now

how to Move
{
if hovering form but not Children
when drag begins
	store relaive position of drag
	show moving cursor ? the cross arrow thing
when drag happens
	move form to new relative position
	update children ?
when drag ends
	thats it
	restore default cursor
}

how to Resize
{
when hovering a resize area
	show resize cursor
when hovering ends, restore default cursor

when drag begins
	check which resize area is being hovered
	left means that left side should follow the cursor, right should stay
	there is some area where hovering from a side is done
	store realtive distanc from cursor
when drag happens
	change Box based on cursor, relative position, and locked side
when drag ends
	resotre default cursor ?
	this should be done by hover end
}
